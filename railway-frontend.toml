# Frontend Service - React/Vite with proper Docker
[build]
builder = "DOCKERFILE"
dockerfilePath = "frontend/Dockerfile"

[env]
VITE_API_URL = "${{services.BACKEND.url}}/api"

[[services]]
name = "frontend"
startCommand = "npm run build && npm run start"
[services.healthChecks]
path = "/"
