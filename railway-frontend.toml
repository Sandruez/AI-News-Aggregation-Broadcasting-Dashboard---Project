# Frontend Service - React/Vite with Caddy
[build]
builder = "DOCKERFILE"
dockerfilePath = "frontend/Dockerfile.prod"

[env]
VITE_API_URL = "${{services.BACKEND.url}}/api"

[[services]]
name = "frontend"
startCommand = "npm run build && npm run start"
[services.healthChecks]
path = "/"
